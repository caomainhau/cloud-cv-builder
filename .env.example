APP_NAME=CloudCV Builder
APP_ENV=local
APP_URL=http://localhost:8080
SESSION_NAME=cloudcv_session
DATABASE_URL=
TRUST_PROXY_HEADERS=false

# Email: use log for local testing. Use resend on Render after configuring API key and sender domain.
MAIL_MODE=log
RESEND_API_KEY=
MAIL_FROM=
REQUIRE_EMAIL_VERIFICATION=false
